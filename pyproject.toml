[project]
name = "fly-in"
version = "0.1.0"
description = "Proot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "numpy",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "flake8",
    "mypy",
]
